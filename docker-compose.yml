version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "54321:54321"
    environment:
      - NODE_ENV=production
      - PORT=54321
    restart: unless-stopped
    volumes:
      # Mount these directories as volumes for development
      # Comment out in production
      - ./backend/src:/app/backend/src
      - ./frontend/src:/app/frontend/src
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:54321/api/modules"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
